server {
    listen 80;
    server_name _;

    # Gzip для статики
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # Статика React-приложения
    root /usr/share/nginx/html;
    index index.html;

    # SPA-роутинг: все пути, которые не найдены на диске, отдаём index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кеширование статических ассетов (Vite добавляет hash в имена файлов)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Проксирование API на бэкенд
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Фото приёмки-передачи авто (2026-07-02, StaticFiles на бэкенде /photos)
    location /photos/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        expires 30d;
        add_header Cache-Control "public";
    }

    # Сканы документов машин (2026-07-03, StaticFiles на бэкенде /truck-scans)
    location /truck-scans/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        expires 30d;
        add_header Cache-Control "public";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
