node_modules/
dist/
__pycache__/
*.pyc
.venv/
*.db
*.tmp.*

# Секреты — не коммитить
.env
.env.staging

# Данные runtime — не коммитить (БД, фото, сканы документов, бэкапы)
# Ведущий / привязывает правило к корню репозитория,
# чтобы не блокировать frontend/src/data/ и подобные вложенные папки.
/data/
/data_staging/

# Логи деплоя
/logs/

# Артефакты сборки Vite и TypeScript
vite.config.ts.timestamp-*.mjs
frontend/vite.config.ts.timestamp-*.mjs
frontend/tsconfig.tsbuildinfo

# Рабочие bat-скрипты (содержат пути/настройки окружения)
crm.bat
deploy.bat
start_docker.bat
